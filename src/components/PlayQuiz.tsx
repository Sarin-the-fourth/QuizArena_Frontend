import type { GetOneQuizResponse } from "@/types/quiz.type";

type PlayQuizProps = {
  data: GetOneQuizResponse;
};

const PlayQuiz = ({ data }: PlayQuizProps) => {
  return <div></div>;
};

export default PlayQuiz;
